/**
 * P125-01 — Files surface identity (supporting evidence only; static operator copy; no inference).
 */

/** Primary label for the Files surface — case-scoped evidence, not the timeline record. */
export const P125_FILES_SURFACE_TITLE = 'Files — Case Evidence';

/**
 * Supporting line: materials for review; separation from the committed timeline; no system-side interpretation.
 */
export const P125_FILES_EVIDENCE_BODY =
	'Documents and attachments here support the investigation. They do not define or replace the committed timeline record. This workspace shows stored files as-is; it does not analyze, interpret, or draw conclusions from their contents.';

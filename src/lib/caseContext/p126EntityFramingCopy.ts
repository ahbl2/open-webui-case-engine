/**
 * P126-01 — Entities surface identity (static operator copy; user-created structured references; no inference).
 */

export const P126_ENTITIES_SURFACE_TITLE = 'Entities — Structured References';

/** Primary doctrine: explicit creation; not the Timeline chronology. */
export const P126_ENTITIES_FRAMING_BODY_PRIMARY =
	'Entities are structured references you create here. They are not Timeline entries and do not replace the committed chronology.';

/** Secondary doctrine: no extraction pipeline; links only when declared. */
export const P126_ENTITIES_FRAMING_BODY_SECONDARY =
	'Nothing is added from Timeline, Notes, or Files without your action. Links exist only when you declare them—no undeclared links.';

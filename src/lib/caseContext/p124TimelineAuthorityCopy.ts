/**
 * P124-01 — Timeline authority framing (static operator copy only; no inference).
 */

/** Primary label for the Timeline surface — official case record. */
export const P124_TIMELINE_SURFACE_TITLE = 'Timeline — Official Case Record';

/**
 * Supporting line: chronological listing and separation from Notes — factual, non-narrative.
 */
export const P124_TIMELINE_AUTHORITY_BODY =
	'Committed entries appear in chronological order by occurrence time. This is the authoritative case record; it is separate from Notes (working drafts).';

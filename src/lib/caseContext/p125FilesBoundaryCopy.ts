/**
 * P125-05 — Surface boundary reinforcement for Files (static operator copy; Files ≠ Timeline ≠ Notes).
 */

/**
 * Supplement to extraction-types help: states Files rows are not Timeline or Notes surfaces.
 */
export const P125_FILES_BOUNDARY_DISCIPLINE_LINE =
	'Files in this workspace are supporting attachments and extracts only — they are not Timeline entries (the committed case record) and not Notes drafts.';
